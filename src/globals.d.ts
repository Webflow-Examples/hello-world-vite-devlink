/** Base path without trailing slash (e.g. "/app"). Use in server-side code. */
declare const __WF_BASE_PATH__: string;
/** Base URL with trailing slash (e.g. "/app/"). Use in client-side code for URL construction. */
declare const __WF_BASE_URL__: string;
