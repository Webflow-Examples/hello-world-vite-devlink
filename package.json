{
  "name": "vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler types && vite",
    "build": "wrangler types && tsc && vite build",
    "preview": "wrangler types && vite preview"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.21.0",
    "hono": "^4.11.7",
    "typescript": "^5",
    "vite": "^7.3.1",
    "wrangler": "^4.59.2"
  }
}
